// src/lib/parent-consent/constants.ts
//
// Values safe to import from client components. service.ts pulls in the
// service-role client and node:crypto, so client code must import from here.

/** Where parents send withdrawal and deletion requests. */
export const GRIEVANCE_EMAIL = "contact.boardedge@gmail.com";
